# v0.8.0-rc.6：保留原请求标准，拒绝回包漂移

本版修复住宅任务与公开检索之间的合同传递问题。例如，原请求要求混合充分性和数量门槛时，执行及回包必须沿用原标准，不能根据已有结果改成质量充分性或删除数量要求。

- 确定性读取住宅请求的验收条件、对象、停止及授权字段；字段重复且矛盾时拒绝继续，不重新推断标准。
- 执行请求保留原件副本和指纹。打包同时核对冻结请求与实际充分性包，新增 `contract_binding`；模式、阈值、质量、多样性、资格或请求身份漂移均可拒绝。
- 旧包可显式按历史结构只读审阅。新版采用要求绑定原请求，旧成果修封装另存新包；保留原证据、冲突、缺口及部分充分状态。
- 区分原范围内迭代、明确禁止继续和超范围扩展。后续授权通过独立 Owner 决定表达，不回改原冻结请求，不替代真实工具权限。
- 新增冻结请求起点的联动检查，避免从回包反建请求后形成虚假的一致性。

配套住宅公开包为 `v0.2.0-rc.8`。公开证据接口仍为 `public_evidence_envelope.v1`，增加绑定字段并更新双方文件校验值；旧标签和既有成果保持可用作历史材料。

公开包 17 组、318 项离线检查通过，包括新增 36 项合同绑定检查；另完成住宅请求到编译、打包和消费的离线联动。检查只证明代码和样例行为，不代表真实检索、异机渠道或业务验收。本次没有启动真实楼盘研究。

安装及旧版升级见 [安装部署提示词](INSTALL_PROMPT_0.8.0-rc.6.md)。首次安装可使用：

```bash
codex plugin marketplace add https://github.com/xiem-public-info/public-info-research-kit.git --ref v0.8.0-rc.6
codex plugin add public-info-research-kit@public-info-research-public
```
