# 更新到0.8.0-rc.8

2026-09-22的rc.8全量包继承rc.7的实际执行绑定，增加公开网页缺口补证、参建关系追读、对象与时点拆解、GUI提交前核对、社媒停止条件及建筑素材方法。完整变更见[rc.8更新说明](RELEASE_NOTES_0.8.0-rc.8.md)。此前本页停留在rc.5的介绍已更新。

首次安装用[同版提示词](INSTALL_PROMPT_0.8.0-rc.8.md)，已有安装用[升级与回退说明](UPGRADE_0.8.0-rc.7_to_rc.8.md)。保留原项目、配置、证据和历史版本；核对实际安装版本、文件清单和新任务加载的七项Skill，不能只看市场显示的版本号。

rc.8为当前最新版本，rc.7作为历史版本保留；既有固定标签和附件不改写。rc.8离线检查与文件完整性通过不代表目标电脑安装、真实渠道或业务验收。安装不自动恢复旧任务，也不迁移账号态或代替用户设置Computer Use权限。

## 2026-09-22 发布口径更正

公开仓采用单一发布口径：发布出去的就是已发布版本，不区分正式版与预发布版。Latest 只表示最新版本。后续版本使用不含 rc、alpha、beta 等阶段后缀的版本号；既有版本号和标签保留，以免影响安装与回退。发布前在本地或未发布草稿中完成必要检查，检查未通过就不发布。发布状态不代替安装、渠道或业务验收。

本次取消既有公开 Release 的预发布标记，rc.8 设为 Latest。固定标签与附件字节不变；旧包内的预发布等字样仅为构建时记录，当前状态以发布页为准。
