# 公开信息研究工具包 rc.6 安装部署提示词

将下面正文复制到目标电脑的 Codex。适用于首次安装或旧版升级。

---

请在这台电脑安装或升级公开信息研究工具包 `public-info-research-kit`，固定版本为 **v0.8.0-rc.6**。

仓库：https://github.com/xiem-public-info/public-info-research-kit

固定版本：https://github.com/xiem-public-info/public-info-research-kit/tree/v0.8.0-rc.6

发布与下载：https://github.com/xiem-public-info/public-info-research-kit/releases/tag/v0.8.0-rc.6

先阅读固定版本的 `README.md`、`RC_INSTALL_0.8.0.md`、`QUICKSTART.md` 和 `RELEASE_NOTES_0.8.0-rc.6.md`，核对实际安装版本和市场来源。已有安装时保留用户配置、任务成果和账号状态；已经是 rc.6 且七项能力可见时直接回报，不重复安装。

优先使用 Codex 的 Plugin 安装或更新入口。需要命令行时先核对本机帮助；首次登记市场的命令为：

```bash
codex plugin marketplace add https://github.com/xiem-public-info/public-info-research-kit.git --ref v0.8.0-rc.6
codex plugin add public-info-research-kit@public-info-research-public
```

已有市场固定旧标签时，先把本市场来源引用调整为 `v0.8.0-rc.6` 再更新本插件。刷新旧标签不能算升级成功，不改动其他市场或插件。

请确认本次修复已加载：

1. 住宅请求中的 `acceptance_contract`、对象、停止条件和授权由编译器映射；执行与回包保留原请求编号、指纹和完整验收条件，不能把混合充分性改成质量充分性或清空原数量门槛。
2. 新研究回包通过原请求与实际充分性输入校验。部分充分、冲突和缺口如实保留，总条目数不能代替合格证据数。
3. 同时使用住宅公开包时，配套版本为 **v0.2.0-rc.8**。两个包各在获准工作目录生成自己的产物，住宅 Owner 决定采用；条件采用不等于真人验收。
4. 保留旧任务和旧回包。旧无绑定回包只能明确按历史结构只读审阅，不能直接进入新版采用；需要修封装时按原请求和已有实际回执另生成新包，不改原请求、不新增检索、不伪造通过。缺原回执则如实说明。
5. 范围内迭代遵从当前有效授权和明确限制；旧任务恢复先回看停止要求，不能把“初始增量未授权”自动改成已授权。新的采用或续作决定另记，原冻结请求不回改。

安装后核实插件实际版本为 `0.8.0-rc.6`，确认七项能力可见：公开任务路由、网页与官方来源、微信已知链接、微信公开研究、小红书可见研究、地图空间证据、公开证据交付。当前任务未刷新能力时提示我新建任务；环境异常时再用默认离线、只读的 doctor。

Computer Use 的安装启用、系统权限和账号登录由我自行完成；不迁移 Cookie、token 或浏览器 profile。安装不启动微信、小红书、真实楼盘或全渠道检索，也不自动恢复旧任务。

最后简要回报实际安装版本、来源、可见能力和需要我处理的环境条件。本版为预发布；离线联动通过不代表这台电脑的真实渠道、用户验收或业务成果已经通过。
