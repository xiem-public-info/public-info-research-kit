# v0.8.0-rc.7：研究回传核对实际执行请求

本版补齐原生及住宅任务从原请求到实际执行、充分性回传和证据封装的检查。原先能按旧 live-gate 或缺实际执行记录通过的路径，须提交本批次的冻结原请求和实际执行请求。

- 实际调用前使用 `--check-execution`；编译成功不再能替代这一步。原生任务保留自己的业务 Owner 和停止要求，缺请求编号时只读沿用 task_id，不改原件。
- 研究充分性校验默认要求原请求和实际 `execution_request`，核对批次、父批次、精确查询与后续授权记录，继续保留原质量、数量、多样性和证据计数标准。
- 后续决定通过独立 `continuation_binding` 绑定请求、完整采用回执、limits 原文和明确预算。住宅公开 rc.8 的原接口及采用回执保持不变；模型和业务 Owner 仍负责正确理解自由文本授权，哈希不能代替这项判断。
- 旧 rc.6 绑定回包及更早格式可以显式 `--historical-read-only` 审阅，始终不报生产绑定通过；原件或哈希篡改仍被拒绝。旧成果保留，缺实际执行记录不自动补检索或伪造回执。
- 安装说明区分市场可用版本、实际安装版本和已加载代码，提供单插件升级及固定 rc.6 回退步骤，保留用户配置和成果。

完整发行回归为 18 组、346 项；新增 28 项执行与历史兼容检查。旧结构行为样例显式只读运行，实际执行样例从冻结原请求编译生成。两包检查使用未修改的住宅 rc.8 消费器及四份实际 JSON Schema，可通过 `scripts/verify_residential_rc8_conformance.py` 重跑。

本版为预发布；公开状态以固定 Release、标签和同版资产为准。离线通过不代表实际 Codex 插件缓存升级、异机渠道或业务验收通过。本轮不启动真实研究，不改变 rc.6 标签、住宅 rc.8 或用户既有项目。

安装或升级见 [同版安装提示词](INSTALL_PROMPT_0.8.0-rc.7.md)、[rc.6 到 rc.7 升级与回退](UPGRADE_0.8.0-rc.6_to_rc.7.md) 和 [中文安装说明](RC_INSTALL_0.8.0.md)。
