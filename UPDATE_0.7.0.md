# 更新与验收 0.7.0

## 更新范围

0.7.0 在 0.6.0 的微信 Lite Stable、小红书可见 Chrome、公开网页、官方文档和 OSM 能力基础上，补齐无需账号的公开动态页与 RSS/公开 Feed 发现路由，并把发行对象明确扩展为陌生公众自助使用。RSS/Feed 仍是 `docs_only` 发现层，不构成长期开启的监控服务；公开动态页仍需逐来源核验。

仍保持七个 Skill，不新增平行入口。机构数据库、土地数据库、明源周报服务、退役的 MediaCrawler/CDP/XHS Searcher、微信 V2/Pad/AX，以及尚未形成现行能力的抖音、视频号和垂直小程序不进入发行包。

## 更新方法

优先在 Codex Plugin 页面点击更新。备用命令见 `QUICKSTART.md`。从 0.4.0、0.5.0 或 0.6.0 更新时无需先卸载；更新完成后新建一个任务。

## 最小验收

1. Plugin 版本显示为 0.7.0，并包含七个 Skill。
2. `doctor.py` 的 `skill_count`、`sensitive_files` 和 `release_manifest_integrity` 为 `pass`；没有安装到当前环境时，`plugin_visibility` 可单独为 `gap`。
3. 路由器可以分别生成微信、小红书、静态公开网页、公开动态页、官方文档、RSS/Feed 和地图空间证据计划。
4. 微信由使用者登录、手动打开公开“搜一搜”，并保持在 Computer Use 当前可操作的主屏执行面；仍只允许一次直接粘贴、一次提交和安全关页。
5. 小红书仍只使用使用者既有、正常且可见的 Chrome；公开动态页不得接管微信或小红书。
6. Feed 条目只作发现候选，必须回到原站后才能形成事实证据；OSM 图件保留清晰可见的 `© OpenStreetMap contributors` 署名。

本验收确认公开包结构、路由和规则接线。账号登录、页面供给和异机真实渠道结果仍需使用者在自己的设备上完成最小任务验证。
