# 公开信息研究工具包 0.8.0-rc.5

本版同步已确认的任务理解与计划校验修正、住宅竞品供给判断和小红书原生入口说明，保留已有检索能力及研究标准。

- 模型结合完整原请求与上下文理解业务范围；代码校验已记录动作与执行计划的一致性，不再用有限句式决定整项任务轻重。明确排除参与校验，通用“QA”“分类”不覆盖业务领域。
- 已识别动作不能被计划遗漏；执行不能违反排除，复用须有引用，暂缓须有原因。纯复用或暂缓计划不产生新检索，查询不能关联已复用或暂缓动作。代码不能发现模型在解释和计划中同时漏掉的要求。
- 首次形成计划、用户增补要求和任务实质变更时回看原请求。未解释部分保留原句，明确且可独立执行的工作继续；不自动增加整套资格审查、搜索或用户填表。
- “已裁定竞品”名单确定研究对象，资格、分类与供给核验按原任务执行。距交付不足 12 个月不再自动降为尾盘；尾盘建议需要目标产品库存与销售阶段依据，缺依据时保留待核。
- 小红书明确从当前 Computer Use 原生 App 入口操作既有可见 Chrome；不使用浏览器接管或 CDP 路径。接口参数错误先查当前说明，按实际画面和调用结果报告。

继续保留任务级渠道授权、微信直接视觉操作、具名链接读取、累计有效成果充分性、空间部分交付、按用途取图、同源去重和下游解释边界。“本轮增量结束”与“原研究充分完成”分别判断。

公开 main、发行分支和 `v0.8.0-rc.5` 标签同步，旧标签与资产保持不变。安装或升级可直接使用同版本的 [安装部署提示词](INSTALL_PROMPT_0.8.0-rc.5.md)；详细步骤见 [中文安装说明](RC_INSTALL_0.8.0.md)。Release 提供 ZIP、提示词和 SHA-256 校验文件。

本版沿用预发布标记。离线回归、发行文件完整性和下载包核对支持本次发行；没有新增真实 GUI、异机安装、第二用户验收或整晚稳定性验证。后续在正常业务中观察原任务进入计划、计划进入工具调用、证据进入回传与成果，复用现有记录。
