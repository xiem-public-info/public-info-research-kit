# 公开信息研究决策手册（0.8 候选）

本手册保留可迁移、可执行的经验结论，不收录研发事故流水、账号态、真实业务样本或已退役实现。遇到下表情形时，先按“归类”处理，再决定继续、纠正、提案或停止。

| 可观察情形 | 归类 | 必须动作 | 禁止动作 |
|---|---|---|---|
| 查询未被正确提交，结果页没有刷新 | `query_transport_failure` | 只按渠道画像允许的一次低风险纠正处理；仍不确定则停止 | 不把它记成语义失败，不连续重复提交 |
| 微信直接粘贴动作报告错误，但最新完整画面逐字显示了完整精确词 | `operator_transient_error_recovered` | 以最新可见画面为准，继续一次提交 | 不二次粘贴，不把动作错误直接等同于界面失败 |
| 微信画面不可观察或状态有歧义 | `route_control_failure` 或 `safety_stop` | 放弃当前查询并保留停止原因 | 不重复危险动作，不启用 V2、Pad、AX、DOM、固定坐标或自动回退 |
| 小红书出现调试、自动化接管、异常 profile 或验证码／风控提示 | `route_control_failure` 或 `safety_stop` | 立即停止并交由终端用户处理 | 不切换到 Playwright、CDP、DOM、隔离 profile 或历史采集器 |
| 列表标题看似相关，但正文未打开 | 尚未形成正文证据 | 继续按冻结的实际开读下限开文 | 不把标题浏览计入 `actual_open_count` |
| 两个确有内容推进的结果批次仍无高质量候选 | `query_semantic_failure`、`identity_collision` 或 `content_supply_gap` | 根据身份、主题和供给现象分层归因；形成下一批提案或停止 | 不只凭“无结果”断言全网没有 |
| 发现同名项目、跨城市对象或历史名混入 | `identity_collision` | 排除、记入 `negative_hits`，重新闭合对象身份 | 不计入合格样本，不把排除项静默丢弃 |
| 当前查询产生大量重复内容且边际增益低 | `content_supply_gap` 或语义效率低 | 记录边际信息增益；只生成 `proposed_incremental_batch` | 未获 `adaptive_extension` 授权不得执行增量 |
| 增量批次已获业务 Owner 授权 | `approved_incremental_batch` | 仍需冻结查询并通过渠道预检、共享桌面和 live Gate | 不把增量授权当成平台执行授权 |
| 某查询在单个任务中表现良好 | `candidate` | 记录匿名化学习回执，等待跨任务验证 | 不直接晋升为推荐模板 |
| 同一业务问题族中跨至少两个真实任务有效，且真人批量复核接受 | `recommended` | 在具名业务问题族中晋升并保留复核回执 | 不跨业务问题族无条件外推 |
| Feed、搜索摘要、转载或 AI 聚合命中 | 发现线索 | 回到官方或真实可见原文后再升级 | 不直接当事实、用户声音或市场比例 |
| 任务同时提到微信 AI 搜索和小红书问点点 | 两个独立的 `platform + aggregate_surface_id` | 分别记录授权、线索和普通原文回源面；普通搜索不得冒充聚合表面 | 不合并成一个“AI 搜索”回执，不跨平台替代 |
| 住宅比较对象距交付不足 12 个月 | 尾盘生命周期信号 | 核验身份、当前供给、近六个月内容、实际可选产品和交付期，只建议下游降为 `tail_end_reference` | 不替下游裁定正式竞品，不直接进入无边界深搜 |
| 首次回传的项目事实、竞品关系、客户选择和反例不能共同支持统一语义核 | `partial` 或 `unsupported` | 返回一份合并增量提案并说明预期信息增益 | 未获 `adaptive_extension` 不执行，不拆成多批连续扩跑 |
| 同一家庭比较多个项目 | 多项目事件归属、单一全局家庭旅程 | 项目归属分别保留，全局独立家庭旅程只计一次 | 不把正文作者与评论者拼成同一家庭链 |
| 官方材料只有摘要，没有必要短原话或原文定位 | 官方表达资产未完成 | 同时保留核心观点、必要短原话、标题／主体／日期／生命周期和原文定位 | 不把摘要标成完整官方表达资产 |
| 当前检索任务仍在进行但线程变慢 | 任务尚未自然闭合 | 先区分页面、GUI 与上下文压力，并完成或安全停止当前任务 | 不催促、不插入下一批、不提前换线程 |
| 只有个人公开表达、营销叙事或平台观察 | `soft_evidence` 或 `platform_observation` | 明确允许用途和 `blocked_use` | 不进入项目硬事实或市场总体判断 |
| 上游已完成证据包 | `fulfilled`、`partial`、`gap` 或 `stopped` | 保持 `downstream_acceptance=not_assessed`，由下游独立决定 | 不把上游完成写成下游接受 |
| 没有冲突或缺口 | 合法空集合 | 分别写 `conflicts: []`、`gaps: []` | 不省略字段，不写 `null` |
| OSM 图件显示清楚，但锚点、POI、坐标或距离未核验 | 仅显示资产可读 | 分别验收空间事实与客户面显示 | 不用“图看起来对”替代空间事实验证 |

所有经验规则都受当次任务范围、终端用户授权、渠道画像、来源许可与停止线约束。离线回归通过只证明合同和拒收逻辑可执行，不证明某台电脑已完成真实渠道 smoke 或业务验收。
