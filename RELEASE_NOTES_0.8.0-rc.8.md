# 0.8.0-rc.8 更新说明

2026-09-22，公开发行全量包。保持七项Skill、既有执行器、请求与住宅互操作接口，更新可迁移的检索方法及操作说明。旧rc.7版本、固定标签和附件保留。

## 本次更新

- 公开网页先宽泛发现，再追正文与关联页；按缺口换资料，不以搜索量或重复转载当增量。
- 参建单位先核关系与标段，再读案例、合同、采购或设计附件；设计意图不升级为实际交付、库存或完整方案。
- 新旧信息先拆栏目、产品、工程范围、事件时点和文书用途；较新整盘记录不替特定产品证明当前状态。
- 下游提供业务判断与关键缺口，检索方安排精确词和任务内迭代；回传材料回答及未回答的范围，采用反馈继续由下游裁定。
- 普通网页／地图GUI先填入、取得新鲜可见画面核对、再单次提交；文本不一致立即停止。共享桌面保持串行，微信／小红书既有路线不变。同步macOS微信主应用绑定说明。
- 明确“两批无高质量候选”是换词／停止条件之一，不是所有检索的最低工作量；不降低任务原有阅读和充分性要求。
- 建筑素材区分真实形态、关系图与生成参考，固定对象编号，按视角缺口选源；不强制单一全景平台，不自动启动建模。

## 更新及验证边界

使用[同版安装提示词](INSTALL_PROMPT_0.8.0-rc.8.md)或[rc.7升级与回退说明](UPGRADE_0.8.0-rc.7_to_rc.8.md)。通用[更新说明](UPDATE_0.8.0.md)、README及安装入口已对齐本版。

本版保持既有Schema和执行工具代码，新增方法资源，不新增平台、自动化执行器或供应商连接器。私有路径、真实客户／项目样本、账号态、内部调度记录和WorkBuddy指导稿未进入发行包。

完整离线回归及发行树检查结果见同包PUBLIC_BETA_MANIFEST.json。本版未新增真实平台、异机安装或业务验收；GUI输入要求不等于运行时互斥或工具故障根因已经修复。发布状态以固定标签Release为准。

## 2026-09-22 发布口径更正

公开仓采用单一发布口径：发布出去的就是已发布版本，不区分正式版与预发布版。Latest 只表示最新版本。后续版本使用不含 rc、alpha、beta 等阶段后缀的版本号；既有版本号和标签保留，以免影响安装与回退。发布前在本地或未发布草稿中完成必要检查，检查未通过就不发布。发布状态不代替安装、渠道或业务验收。

本次取消既有公开 Release 的预发布标记，rc.8 设为 Latest。固定标签与附件字节不变；旧包内的预发布等字样仅为构建时记录，当前状态以发布页为准。
